
export interface UserForm {
    fname: string;
    email: string;
    contact: string;
    dob: string;
  }

  export interface User{
    id?: number,
    fname?: string,
    email?: string,
    password?: string
    contact?: string
    dob?: string
}
